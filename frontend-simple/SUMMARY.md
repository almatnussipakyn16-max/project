# Frontend-Simple - Сводка по проекту

## 📊 Статистика

- **Всего файлов:** 64 файла
- **TypeScript/React компоненты:** 52 файла
- **Строк кода:** ~4,200+ строк
- **Размер bundle (production):** 305.81 kB JS + 18.08 kB CSS
- **Статус TypeScript:** ✅ Компилируется без ошибок
- **Статус Production Build:** ✅ Успешно

## 🎨 Архитектура

### API Клиенты (10 модулей)
```
src/api/
├── client.ts           # Axios с JWT interceptors
├── types.ts            # TypeScript типы (250+ строк)
├── auth.ts             # Аутентификация
├── restaurants.ts      # Рестораны
├── menu.ts             # Меню
├── orders.ts           # Заказы
├── reservations.ts     # Бронирования
├── payments.ts         # Платежи
├── promotions.ts       # Промокоды
├── notifications.ts    # Уведомления
└── support.ts          # Поддержка
```

### UI Компоненты (18 компонентов)
```
src/components/
├── common/             # 6 общих компонентов
│   ├── Button.tsx
│   ├── Input.tsx
│   ├── Card.tsx
│   ├── Badge.tsx
│   ├── Spinner.tsx
│   └── Modal.tsx
├── layout/             # 2 layout компонента
│   ├── Header.tsx
│   └── Footer.tsx
├── restaurant/         # 2 компонента
│   ├── RestaurantCard.tsx
│   └── RestaurantList.tsx
├── menu/               # 2 компонента
│   ├── MenuItem.tsx
│   └── MenuCategory.tsx
├── cart/               # 2 компонента
│   ├── CartItem.tsx
│   └── CartSummary.tsx
└── order/              # 2 компонента
    ├── OrderCard.tsx
    └── OrderStatus.tsx
```

### Страницы (14 страниц)
```
src/pages/
├── Home.tsx                    # Главная
├── auth/
│   ├── Login.tsx              # Вход
│   └── Register.tsx           # Регистрация
├── restaurants/
│   ├── List.tsx               # Список ресторанов
│   └── Detail.tsx             # Детали + меню
├── cart/
│   └── Cart.tsx               # Корзина
├── checkout/
│   └── Checkout.tsx           # Оформление
├── orders/
│   ├── List.tsx               # Мои заказы
│   └── Detail.tsx             # Детали заказа
├── reservations/
│   ├── List.tsx               # Мои брони
│   └── Create.tsx             # Создать бронь
├── profile/
│   └── Profile.tsx            # Профиль
└── support/
    ├── Tickets.tsx            # Тикеты
    └── Create.tsx             # Создать тикет
```

### State Management
```
src/store/
├── auth.ts             # Zustand store для аутентификации
└── cart.ts             # Zustand store для корзины
```

### Утилиты
```
src/utils/
├── constants.ts        # Тексты на русском языке
├── formatters.ts       # Форматирование данных
└── validators.ts       # Валидация форм
```

### Hooks
```
src/hooks/
├── useAuth.ts          # Hook для аутентификации
├── useCart.ts          # Hook для корзины
└── useDebounce.ts      # Hook для debounce
```

## 🚀 Запуск

```bash
# Установка
npm install

# Разработка
npm run dev          # → http://localhost:5174

# Production
npm run build        # → dist/
npm run preview      # Предпросмотр

# TypeScript check
npx tsc --noEmit
```

## 🔑 Ключевые особенности

### 1. Аутентификация с JWT
- Автоматическое обновление токенов
- Interceptors для добавления токенов
- Защищённые маршруты
- Logout с очисткой

### 2. State Management
- **Zustand** для auth и cart (персистентность в localStorage)
- **TanStack Query** для API данных (кэширование, автоматическое обновление)

### 3. Дизайн
- **Mobile-first** responsive
- **Tailwind CSS** с кастомной цветовой схемой
- **Orange (#f97316)** как primary цвет
- Темы: success, error, warning
- Адаптивная навигация

### 4. Производительность
- Code splitting по маршрутам
- Lazy loading компонентов
- Оптимизированные изображения
- Debounce для поиска
- Кэширование API

### 5. TypeScript
- Полная типизация
- Interfaces для всех API моделей
- Props типы для компонентов
- Type safety

## 📱 Функционал по страницам

### Главная (/)
- Hero section с поиском
- Популярные рестораны (первые 6)
- Категории услуг
- CTA секция

### Рестораны (/restaurants)
- Поиск по названию (debounced)
- Фильтр по типу кухни
- Карточки ресторанов
- Рейтинг, отзывы, доставка

### Детали ресторана (/restaurants/:id)
- Полная информация
- Меню по категориям
- Добавление в корзину
- Фото, рейтинг, контакты

### Корзина (/cart)
- Список товаров
- Количество ± кнопки
- Удаление позиций
- Промокоды
- Расчёт суммы

### Оформление (/checkout) 🔒
- Выбор типа заказа (доставка/самовывоз)
- Адрес доставки
- Способ оплаты
- Подтверждение

### Мои заказы (/orders) 🔒
- Список всех заказов
- Фильтр по статусу
- Карточки заказов

### Детали заказа (/orders/:id) 🔒
- Полная информация о заказе
- Progress bar статуса
- Позиции заказа
- Адрес доставки
- Кнопка отмены (если PENDING/CONFIRMED)

### Бронирования (/reservations) 🔒
- Список броней
- Статусы броней
- Информация о столиках

### Создать бронь (/reservations/create) 🔒
- Выбор ресторана
- Дата и время
- Количество гостей
- Особые пожелания

### Профиль (/profile) 🔒
- Личные данные
- Дата регистрации
- Быстрые ссылки

### Поддержка (/support) 🔒
- Список тикетов
- Статусы и приоритеты
- Категории

### Создать тикет (/support/create) 🔒
- Тема, описание
- Категория
- Приоритет

## 🔌 API Интеграция

Все endpoints используют базовый URL из `.env`:

```bash
VITE_API_URL=http://localhost:8000/api
```

### Реализованные endpoints:

1. **Auth** - `/api/auth/`
   - POST `/jwt/create/` - Вход
   - POST `/users/` - Регистрация
   - GET `/users/me/` - Текущий пользователь
   - POST `/jwt/refresh/` - Обновление токена

2. **Restaurants** - `/api/restaurants/`
   - GET `/` - Список (с поиском, фильтрами)
   - GET `/:id/` - Детали
   - GET `/:id/menu/` - Меню

3. **Menu** - `/api/menu/`
   - GET `/categories/` - Категории
   - GET `/items/` - Позиции

4. **Orders** - `/api/orders/`
   - GET `/` - Список
   - GET `/:id/` - Детали
   - POST `/` - Создать
   - PATCH `/:id/` - Обновить (отменить)

5. **Reservations** - `/api/reservations/`
   - GET `/` - Список
   - GET `/:id/` - Детали
   - POST `/` - Создать
   - PATCH `/:id/` - Обновить

6. **Support** - `/api/support/tickets/`
   - GET `/` - Список
   - POST `/` - Создать
   - GET `/:id/` - Детали

7. **Promotions** - `/api/promotions/`
   - GET `/` - Список
   - POST `/orders/:id/apply-coupon/` - Применить

8. **Payments** - `/api/payments/`
   - GET `/` - Список
   - POST `/` - Создать

9. **Notifications** - `/api/notifications/`
   - GET `/` - Список
   - PATCH `/:id/` - Отметить прочитанным

## 🌍 Русификация

Все тексты на русском языке в `src/utils/constants.ts`:

- Навигация
- Формы
- Сообщения об ошибках
- Статусы
- Кнопки
- Плейсхолдеры

## 🎨 Цветовая схема

```javascript
primary: {
  500: '#f97316',  // Orange
  600: '#ea580c',
}
success: '#10b981'
error: '#ef4444'
warning: '#f59e0b'
```

## 📦 Зависимости

### Runtime
- react 18.3.1
- react-dom 18.3.1
- react-router-dom 6.22.0
- @tanstack/react-query 5.28.0
- axios 1.6.7
- zustand 4.5.2

### DevDependencies
- typescript 5.3.3
- vite 5.1.4
- @vitejs/plugin-react 4.2.1
- tailwindcss 3.4.1
- autoprefixer 10.4.17
- postcss 8.4.35

## ✅ Готово к использованию

Проект полностью готов к работе:
- ✅ TypeScript без ошибок
- ✅ Production build успешно
- ✅ Все зависимости установлены
- ✅ Документация готова
- ✅ Код структурирован
- ✅ API интеграция полная

## 🚀 Следующие шаги

1. Запустить backend на `http://localhost:8000`
2. Создать `.env` файл с URL backend
3. Запустить `npm run dev`
4. Открыть `http://localhost:5174`
5. Начать использование!
